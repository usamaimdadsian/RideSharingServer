import express, { Router } from 'express';
import { <controller_name> } from './<controller_name>';

export const <name>Router = Router();
const <name>Controller = new <controller_name>();

<name>Router.get('/', <name>Controller.index);
<name>Router.get('/:<name>Id', <name>Controller.show);
<name>Router.post('/', <name>Controller.create);
<name>Router.patch('/:<name>Id', <name>Controller.update);
<name>Router.delete('/:<name>Id', <name>Controller.delete);
